"use client";
import {useState} from "react";
export function ChatAssistant(){const[open,setOpen]=useState(false);const[text,setText]=useState("");const[messages,setMessages]=useState(["Hi! I’m Zia, your ZetaZAI assistant. Ask about plans, projects, agents, or payments."]);function send(){if(!text.trim())return;const q=text;setMessages(x=>[...x,`You: ${q}`,`Zia: I can help you ${/plan|price/i.test(q)?"compare plans starting at ৳300/month":/agent/i.test(q)?"create up to three included agents, then upgrade for more":/project/i.test(q)?"create a project, attach documents, and apply AI tools":"find the right ZetaZAI workflow"}.`]);setText("")}return <div className={`zia ${open?"open":""}`}><button className="zia-launch" onClick={()=>setOpen(!open)} aria-label="Open ZetaZAI assistant">{open?"×":"✦"}</button>{open&&<section><header><b>Zia</b><span>● Online assistant</span></header><div className="zia-messages">{messages.map((x,i)=><p className={x.startsWith("You:")?"user":""} key={i}>{x}</p>)}</div><form onSubmit={e=>{e.preventDefault();send()}}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Ask Zia anything…"/><button>➤</button></form></section>}</div>}
